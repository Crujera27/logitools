<!DOCTYPE html>
<html class="h-100" lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= app.dashboard_name %> - <%= t.pages.verify %></title>
    <link rel="stylesheet" href="/public/css/style.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />
</head>
<body class="d-flex flex-column h-100">
    <main class="flex-shrink-0">
        <div class="container min-vh-100 d-flex align-items-center justify-content-center">
            <div class="verification-box col-md-6 col-lg-4">
                <h2 class="mb-4"><%= t.verify.title %></h2>
                <form id="verificationForm">
                    <div class="mb-3">
                        <label for="verificationCode" class="form-label"><%= t.verify.verification_code %>:</label>
                        <input type="text" class="form-control" id="verificationCode" name="verificationCode" required>
                    </div>
                    <button type="submit" class="btn btn-primary w-100"><%= t.verify.verify_button %></button>
                    <div class="additional-info mt-4">
                        <p><i class="fa-solid fa-info"></i> <%= t.verify.security_message.replace('{dashboard}', '<span style="color: plum;">Logidash</span>') %></p>
                        <p><i class="fa-solid fa-user-tie"></i> <%= t.verify.logged_as.replace('{username}', `<span style="color: plum;">${user.username}</span>`) %></p>
                    </div>
                </form>
            </div>
        </div>
    </main>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="script.js"></script>
</body>
</html>