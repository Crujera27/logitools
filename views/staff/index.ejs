<!DOCTYPE html>
<html>
  <head>
    <title><%= app.dashboard_name %> - <%= t.pages.staff_portal %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"/>
    <link rel="stylesheet" href="/public/css/style.css" />
  </head>

  <body class="bg-dark text-light">
    <%- include('../inc/staff-navbar.ejs') %>
    
    <div class="container mt-5">
      <div class="row">
        <div class="col-12">
          <h1 class="display-4">
            <%= user.discriminator==="0" ? 
              t.pages.staff_greeting.replace('{username}', user.username) :
              t.pages.staff_greeting.replace('{username}', `${user.username}#${user.discriminator}`) %>
          </h1>
          <p class="lead"><%= t.pages.staff_welcome_message %></p>
        </div>

        <div class="col-12 col-md-6">
          <div class="card bg-secondary mb-4">
            <div class="card-body">
              <h2 class="card-title h4 mb-3"><%= t.sections.resources %></h2>
              <div class="d-flex flex-wrap gap-2">
                <a href="https://github.com/Crujera27/logitools" class="btn btn-dark">GitHub</a>
                <a href="https://github.com/Crujera27/logitools" class="btn btn-dark"><%= t.sections.documentation %></a>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>